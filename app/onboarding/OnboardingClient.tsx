"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { ArrowLeft, ArrowRight, Bike, Check, ShoppingBag, Store } from "lucide-react";

export default function OnboardingClient({ name }: { name: string }) {
  const [step,setStep]=useState(1); const [role,setRole]=useState("customer"); const [phone,setPhone]=useState(""); const [city,setCity]=useState("Douala"); const [address,setAddress]=useState(""); const [businessName,setBusinessName]=useState(""); const [businessCategory,setBusinessCategory]=useState("Restaurant"); const [vehicleType,setVehicleType]=useState("motorcycle"); const [busy,setBusy]=useState(false); const [error,setError]=useState(""); const router=useRouter();
  const finish=async()=>{setBusy(true);setError("");const response=await fetch("/api/workspace",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"complete_onboarding",role,phone,city,address,businessName,businessCategory,vehicleType})});if(!response.ok){const data=await response.json();setError(data.error||"Registration failed");setBusy(false);return}router.push("/dashboard")};
  return <main className="registration"><header><a className="pro-logo" href="/"><span>k</span>kola</a><span>Account setup</span></header><div className="registration-shell"><aside><span className={step>=1?"active":""}><i>{step>1?<Check size={13}/>:1}</i><b>Choose your account</b></span><span className={step>=2?"active":""}><i>{step>2?<Check size={13}/>:2}</i><b>Contact details</b></span><span className={step>=3?"active":""}><i>3</i><b>{role==="vendor"?"Business details":role==="rider"?"Delivery profile":"Delivery address"}</b></span></aside><section><p className="reg-kicker">WELCOME, {name.toUpperCase()}</p>{step===1&&<><h1>How will you use Kola?</h1><p className="reg-subtitle">Choose the account that matches what you want to do. This controls your workspace and permissions.</p><div className="reg-roles"><button className={role==="customer"?"active":""} onClick={()=>setRole("customer")}><ShoppingBag/><div><b>Customer</b><span>Shop and track deliveries</span></div></button><button className={role==="vendor"?"active":""} onClick={()=>setRole("vendor")}><Store/><div><b>Business</b><span>Sell products and fulfil orders</span></div></button><button className={role==="rider"?"active":""} onClick={()=>setRole("rider")}><Bike/><div><b>Delivery rider</b><span>Accept and complete deliveries</span></div></button></div></>}{step===2&&<><h1>Your contact details</h1><p className="reg-subtitle">We use these details for order updates and delivery coordination.</p><label>Mobile number<span>Required</span></label><div className="phone-field production"><span>+237</span><input inputMode="tel" value={phone} onChange={e=>setPhone(e.target.value.replace(/[^\d ]/g,""))} placeholder="6XX XXX XXX"/></div><small className="field-help">Your identity is secured by your ChatGPT sign-in. SMS verification will be added when the production provider is connected.</small><label>City<span>Required</span></label><select value={city} onChange={e=>setCity(e.target.value)}><option>Douala</option><option>Yaoundé</option><option>Bafoussam</option><option>Bamenda</option><option>Limbe</option><option>Other</option></select></>}{step===3&&<><h1>{role==="vendor"?"Set up your business":role==="rider"?"Your delivery profile":"Where should we deliver?"}</h1><p className="reg-subtitle">{role==="vendor"?"You can change these details later from store settings.":role==="rider"?"Your profile will be reviewed before delivery payouts are enabled.":"Landmarks and access instructions help riders find you."}</p>{role==="vendor"&&<><label>Business name<span>Required</span></label><input value={businessName} onChange={e=>setBusinessName(e.target.value)} placeholder="e.g. Chez Mado"/><label>Business type</label><select value={businessCategory} onChange={e=>setBusinessCategory(e.target.value)}><option>Restaurant</option><option>Grocery</option><option>Fashion</option><option>Pharmacy</option><option>Retail</option><option>Services</option></select><label>Pickup address<span>Required</span></label><textarea value={address} onChange={e=>setAddress(e.target.value)} placeholder="Street, neighbourhood and nearby landmark"/></>}{role==="rider"&&<><label>Vehicle type<span>Required</span></label><select value={vehicleType} onChange={e=>setVehicleType(e.target.value)}><option value="motorcycle">Motorcycle</option><option value="bicycle">Bicycle</option><option value="car">Car</option><option value="van">Van</option></select><div className="info-panel">Vehicle and identity documents are required before your account can receive customer deliveries.</div></>}{role==="customer"&&<><label>Default delivery address<span>Required</span></label><textarea value={address} onChange={e=>setAddress(e.target.value)} placeholder="Street, neighbourhood, landmark and gate details"/></>}</>}{error&&<div className="form-error">{error}</div>}<div className="reg-actions">{step>1&&<button className="back-button" onClick={()=>setStep(s=>s-1)}><ArrowLeft size={16}/>Back</button>}<button className="blue-button large" disabled={busy||(step===2&&phone.replace(/\s/g,"").length<8)||(step===3&&role!=="rider"&&!address.trim())||(step===3&&role==="vendor"&&!businessName.trim())} onClick={()=>step<3?setStep(s=>s+1):finish()}>{busy?"Creating account…":step<3?<>Continue <ArrowRight size={16}/></>:<>Finish setup <ArrowRight size={16}/></>}</button></div></section></div></main>;
}
